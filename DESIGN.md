# 隔离方案重新设计

## 一、先认错：机制选型的问题出在哪

当前方案用 **bubblewrap 挂载命名空间**做隔离。它要求内核允许**非特权 user
namespace**，而这个开关在宿主机上，容器内改不了。

问题不在于 bwrap 不好，而在于**我选它之前没有确认这个前提是否成立，而这个
前提恰好与本项目「rootless 容器部署」的基本约束相冲突**。更糟的是我在
`dsh-runtime/dsh-sandbox.sh:153` 的日志里写下了「这是宿主机的内核设置，
容器内改不了」——也就是说我意识到了，却没有让它改变方案，而是继续往上加
了 netns、unix socket 等一层层东西。这是判断失误。

代价：现场部署下来，隔离一天也没生效过，实例一直以 `DSH_ALLOW_UNCONFINED=1`
无隔离运行。功能全通掩盖了核心目标没达成。

## 二、根本约束（这一条决定了所有方案的形状）

在**单个容器、所有 dsh 实例同为一个 OS 用户、无命名空间**这三条同时成立时，
**员工之间的隔离在数学上就不可能**：

- 文件权限（DAC）以 UID 为单位，两个同 UID 的进程在内核眼里没有区别
- 应用层限制（目录选择器钳制）挡不住 dsh 的 bash 工具，它可以用绝对路径直接读写
- 于是没有任何强制点

**能做强制点的只有三种边界**，每一种都需要一个前提：

| 边界 | 需要什么 |
|------|---------|
| 命名空间 | 非特权 userns（宿主放行）或容器内 root |
| 不同 UID | **容器内** root（能 `useradd`） |
| 不同容器 | 能创建兄弟容器（docker socket 等） |

三条都拿不到的话，结论就是「做不到」，剩下的只能是把产品定义收缩。任何声称
在这种条件下实现了隔离的方案都是自欺。

## 三、三条可行路线

### 方案 A：一员工一容器

每个员工的 dsh 跑在自己的容器里，只挂载他自己的目录。隔离由容器边界提供，
由宿主的容器运行时强制——那是本来就被信任的边界。

- **隔离强度**：最强。文件、进程、网络天然分开
- **前提**：能创建兄弟容器（docker socket / compose / k8s）
- **改造量**：中。`dsh-sandbox.sh` 换成 `docker run` 包装（约 100 行），
  nginx 改为路由到各容器；`core.py` 只需换 upstream 形式
- **附带好处**：网络也自然隔离了，`DSH_NETNS` 那套 pasta + unix socket 可以整个删掉
- **代价**：每实例一个容器，内存与启动开销上升；镜像与版本管理多一层

### 方案 B：容器内一员工一 OS 用户

在容器**内部**给每个员工建一个 unix 用户，dsh 以该用户身份运行，工作区
`chown` 给他、`chmod 700`。隔离由文件属主提供，**完全不需要命名空间**。

- **隔离强度**：文件层强制，内核级
- **前提**：**容器内 root**（能 `useradd` / `setpriv`）——注意这比「宿主 root」
  弱得多，很多容器默认就是 root 起的
- **改造量**：小到中。`dsh-sandbox.sh` 的 bwrap 调用换成 `setpriv --reuid`
  （约 60 行）；登记表加 uid 字段；建号时 chown 工作区；FileBrowser 与 admin
  需要一个能读全部目录的 gid
- **残留**：员工 A 仍能连 B 的 dsh 端口（同一网络命名空间）。但此时 B 的
  dsh 只能动 B 的文件，破坏面被限制住了
- **注意**：`initial-credentials.txt`、TLS 私钥这些也要 chown 到只有 admin 能读

### 方案 C：宿主放行 userns（= 现有代码）

- **隔离强度**：强（文件系统完全隔离，已在 CI 里 21 项断言验证）
- **前提**：宿主执行 `scripts/apparmor-allow-userns.sh`，或容器 run 时加
  `--security-opt apparmor=unconfined`
- **改造量**：**零**，代码已经就位
- **代价**：需要宿主侧配合一次

## 四、怎么选：先确认三条事实

这三条我在这边测不了，但每条一条命令：

```bash
# 1. 容器内是不是 root？ -> 决定方案 B 是否可行（最重要，因为不求宿主）
id -u                                    # 0 = 是

# 2. 能不能起兄弟容器？ -> 决定方案 A 是否可行
ls -l /var/run/docker.sock 2>/dev/null && docker ps >/dev/null 2>&1 && echo "可以"

# 3. 拿不拿得到宿主？ -> 决定方案 C 是否可行
#    （此前 robot@robot-pc 上 sudo 是能用的，需要确认那台就是宿主还是另一层）
```

决策顺序建议：

```
容器内是 root？ ──是──> 方案 B（不求人，最快落地）
      │否
      ▼
有 docker socket？ ──是──> 方案 A（隔离最强，架构最正）
      │否
      ▼
宿主愿意放行 userns？ ──是──> 方案 C（代码已就绪，改一次配置）
      │否
      ▼
方案 D：承认做不到，收缩产品定义（见下）
```

> 一个尚未澄清的点：此前 `robot@robot-pc` 上 `sudo` 是可用的，而 `ss -ltnp`
> 在那台机器上直接看得到 dsh 与 nginx 进程。如果 robot-pc 就是宿主、服务直接
> 跑在上面（而非容器内），那方案 C 只差一条命令。这一点值得先确认清楚，
> 因为它可能让整个讨论变得多余。

## 五、方案 D：如果三条前提都不成立

那就不要假装有隔离。诚实的收缩方式，按推荐度排序：

1. **只保留文件服务器的多租户**。FileBrowser 在进程内做 scope 检查，
   这一层是真的（员工拿不到 shell）。dsh 则**只给 admin 用**，或全员共用
   一个实例并明确告知「工作台内无隐私」
2. **dsh 实例不给 bash / 文件工具**（若 dsh 支持裁剪工具集）。没有任意
   文件读写能力时，目录选择器钳制就从「UX 装饰」变成了真边界
3. **接受现状但如实标注**：员工的 dsh 等同于给了他一个服务器 shell。
   那么 `initial-credentials.txt` 必须删除、TLS 私钥必须移出容器、
   FileBrowser 的密钥头必须保留（这条已在现网验证有效）

## 六、无论选哪条，现在就该做的

与隔离机制无关、且当前无隔离状态下尤其要紧的：

- 删掉 `dsh-auth/initial-credentials.txt`（全员明文初始密码）。管理后台
  已改为「建号时显示一次、不落盘」，这个历史文件没有存在必要
- TLS 私钥考虑移到容器外由宿主 nginx 持有
- 保留 FileBrowser 与管理后台的密钥头机制（现网已验证能挡住伪造 admin）
- `preflight-sandbox.sh` 作为唯一判据：**功能全通 ≠ 隔离生效**

## 七、现有代码的可迁移性

隔离机制集中在 451 行三个文件里，接口是稳定的：

```
dsh-sandbox.sh <username> <port> <dsh_home> <workspace>
```

`core.py` 里只有约 20 行与之耦合。换成方案 A 或 B，改的是这个脚本的内部实现，
上层的建号引擎、管理后台、空间模型（`space_for()`）、nginx 路由、99 项测试
全部不动。当初把它做成一个独立脚本而不是散落在各处，现在派上用场了。
