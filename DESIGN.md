# 隔离方案重新设计

## 一、先认错：机制选型的问题出在哪

当前方案用 **bubblewrap 挂载命名空间**做隔离。它要求内核允许**非特权 user
namespace**，而这个开关在宿主机上，容器内改不了。

问题不在于 bwrap 不好，而在于**我选它之前没有确认这个前提是否成立，而这个
前提恰好与本项目「rootless 容器部署」的基本约束相冲突**。更糟的是我在
`dsh-runtime/dsh-sandbox.sh:153` 的日志里写下了「这是宿主机的内核设置，
容器内改不了」——也就是说我意识到了，却没有让它改变方案，而是继续往上加
了 netns、unix socket 等一层层东西。这是判断失误。

代价：现场部署下来，隔离一天也没生效过，实例一直以 `DSH_ALLOW_UNCONFINED=1`
无隔离运行。功能全通掩盖了核心目标没达成。

**结论见第三节：改用独立容器。** 环境里既然有 docker，这是唯一一条前提已经
满足、且不需要求任何人配合的路。

## 二、根本约束（这一条决定了所有方案的形状）

在**单个容器、所有 dsh 实例同为一个 OS 用户、无命名空间**这三条同时成立时，
**员工之间的隔离在数学上就不可能**：

- 文件权限（DAC）以 UID 为单位，两个同 UID 的进程在内核眼里没有区别
- 应用层限制（目录选择器钳制）挡不住 dsh 的 bash 工具，它可以用绝对路径直接读写
- 于是没有任何强制点

**能做强制点的只有三种边界**，每一种都需要一个前提：

| 边界 | 需要什么 |
|------|---------|
| 命名空间 | 非特权 userns（宿主放行）或容器内 root |
| 不同 UID | **容器内** root（能 `useradd`） |
| 不同容器 | 能创建兄弟容器（docker socket 等） |

三条都拿不到的话，结论就是「做不到」，剩下的只能是把产品定义收缩。任何声称
在这种条件下实现了隔离的方案都是自欺。

## 三、结论：用独立容器，不要用用户组

环境已确认：**容器内有 docker，可以套容器**。这一条直接定下了方案——

### 为什么不是用户组（UID/GID）

UID 隔离要在这个环境里成立，必须**同时**满足下面五条，错一条就全盘失守：

| 必须满足 | 独立容器怎么样 |
|---------|--------------|
| 每个 dsh 以独立 UID 运行 | 天然 |
| 该 UID 摸不到 `/var/run/docker.sock` | 不挂进去即可，只此一条 |
| 员工 A 连不到员工 B 的 dsh 端口 | 天然（各自网络命名空间） |
| 容器内无 setuid 提权路径 | 天然 |
| `/tmp`、`/proc` 等共享面收干净 | 天然 |

第二条是决定性的。容器里有 docker，员工的 dsh 又有 shell，只要够得到
docker socket 就是一句话逃逸：

```bash
docker run -v /:/host alpine cat /host/etc/shadow
```

UID 隔离在这条面前完全无效。**UID 机制是为「互相协作的用户」设计的，不是
为「互相敌对、且每人手握 shell 的租户」设计的**——本项目每个 dsh 都是能执行
任意命令的 agent，属于后者。

### 目标架构

```
主容器（现有）
├── nginx        8099/9091   唯一出口，路由不变
├── Authelia     19091
├── FileBrowser  18080       挂载 dsh-files 全量，进程内做 scope 检查
├── 管理后台      19200       持 docker 客户端，负责起停员工容器
└── docker
    ├── dsh-zhangsan   只挂 departments/研发部/张三   -p 127.0.0.1:13101:3000
    ├── dsh-lisi       只挂 departments/研发部        -p 127.0.0.1:13102:3000  （主管）
    └── dsh-admin      挂 dsh-files 全量              -p 127.0.0.1:3080:3000
```

四个要点：

1. **员工容器绝不挂 docker socket**。这是整个方案唯一必须守住的一条，
   也是它相对 UID 方案的全部优势所在。
2. **端口必须映射到回环**：`-p 127.0.0.1:13101:3000`。这样 nginx 连得到，
   而员工容器之间连不到——各自有独立 netns，容器内的 `127.0.0.1` 是它自己的；
   宿主端口只在回环监听，容器经网关 IP 也够不着。**实例间互连问题就此消失**，
   此前为它做的 pasta + unix socket 那一整套可以全部删掉。
3. **nginx 几乎不用改**。`map $user $dsh_upstream` 仍然产出
   `127.0.0.1:<port>`，与现在一模一样。
4. **顺带拿到现在完全没有的东西**：`--memory` / `--cpus` 资源限额（现在一个
   员工跑爆内存会拖垮所有人）、`--restart` 策略、独立日志、镜像版本管理。

### 落地时第一个会踩的坑：volume 路径

容器里的 docker 有两种形态，volume 路径的写法完全不同：

```bash
# 判断：docker socket 是挂进来的（sibling），还是容器内自己跑的 dockerd（DinD）
ls -l /var/run/docker.sock && docker info --format '{{.Name}}'
```

- **sibling（挂宿主 socket，最常见）**：起出来的是**宿主的兄弟容器**，
  `-v` 的左边必须是**宿主上的绝对路径**，写当前容器内的路径会挂载失败或挂空目录。
  需要知道 `dsh-files` 在宿主上的真实位置，通常通过环境变量注入
  （如 `DSH_FILES_HOST_ROOT`），由 core.py 在拼 `docker run` 时换算。
- **DinD（容器内自己的 dockerd）**：路径就是当前容器内的路径，不用换算。

这一条不搞清楚，容器能起来但挂载是错的，比起不来更难查。

### 改造量

隔离机制集中在 451 行三个文件里，接口稳定，`core.py` 只有约 20 行耦合：

| 动作 | 内容 |
|------|------|
| 新增 | `dsh-container.sh`——`docker run` 包装，替换 `dsh-sandbox.sh` 的位置 |
| 改 | `core.py` 的启停改为 docker 命令；登记表加容器名；加宿主路径换算 |
| 删 | bwrap 那套、`dsh-netns-entry.sh`、pasta/socat、`DSH_NETNS` 开关 |
| 不动 | nginx 路由、空间模型 `space_for()`、建号引擎、管理后台、密钥头机制 |
| 测试 | 隔离断言从「沙箱内 cat 不到」改为「容器内 cat 不到」，断言本身照搬 |

### 备选：如果哪天 docker 不可用

按前提由弱到强：容器内 root → UID 隔离（需补齐上表五条）；
宿主可配合 → 放行 userns 走原有 bwrap 方案（代码还在，`DSH_NETNS` 一并保留）。
两者都拿不到时见第五节。

## 六、无论选哪条，现在就该做的

与隔离机制无关、且当前无隔离状态下尤其要紧的：

- 删掉 `dsh-auth/initial-credentials.txt`（全员明文初始密码）。管理后台
  已改为「建号时显示一次、不落盘」，这个历史文件没有存在必要
- TLS 私钥考虑移到容器外由宿主 nginx 持有
- 保留 FileBrowser 与管理后台的密钥头机制（现网已验证能挡住伪造 admin）
- `preflight-sandbox.sh` 作为唯一判据：**功能全通 ≠ 隔离生效**

## 七、现有代码的可迁移性

隔离机制集中在 451 行三个文件里，接口是稳定的：

```
dsh-sandbox.sh <username> <port> <dsh_home> <workspace>
```

`core.py` 里只有约 20 行与之耦合。换成方案 A 或 B，改的是这个脚本的内部实现，
上层的建号引擎、管理后台、空间模型（`space_for()`）、nginx 路由、99 项测试
全部不动。当初把它做成一个独立脚本而不是散落在各处，现在派上用场了。
